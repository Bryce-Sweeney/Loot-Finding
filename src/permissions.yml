lootPlug.use:
  description: Can use the lootFind plugin.
  default: true
  children:
    lootSelector.use:
      description: Can use the loot selector tool.
      default: op
    lootFinder.use:
      description: Can use the loot finder tool.
      default: true